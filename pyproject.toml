[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "git_helpers"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
git_helpers = "git_helpers.cli_utils:main"

[tool.hatch.build.targets.wheel]
packages = ["src/git_helpers"]
