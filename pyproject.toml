[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "githelpers"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
git_helpers = "githelpers.cli_utils:main"

[tool.hatch.build.targets.wheel]
packages = ["src/githelpers"]
